anthropic>=0.97.0
requests>=2.31.0
PyYAML>=6.0
# onlinejobs.ph has no API — it is scraped HTML. Regex is fine for flattening a
# description fragment (sources/base.strip_html) but not for pulling structured
# records out of a 190KB page that is expected to drift.
beautifulsoup4>=4.12.0
lxml>=5.0.0
